{# Quizontal: storefront URL used by the order page domain search card.
   deploy/install-interserver-provisioning.sh REGENERATES this file on every
   deploy using STOREFRONT_URL (or APP_URL from the repo .env).
   An empty URL keeps the built-in in-place domain order form as fallback. #}
{% macro url() %}{% endmacro %}
